{
  "name": "nodeveil-desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test test/state.test.mjs",
    "format": "prettier --write .",
    "build": "tsc -p tsconfig.json"
  },
  "packageManager": "pnpm@10.18.3"
}
